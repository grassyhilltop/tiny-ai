
# Large datasets — distributed via each lab's code zip, not committed to git.
# NSL-KDD full splits for Lab 16 (the 4,000-row sample IS committed as a fallback).
# Python
# Trained model artifacts (regenerated by the lab code)
# macOS
*.pkl
*.pyc
.DS_Store
.venv/
__pycache__/
dist/
labs/lab-16/data/KDDTest+.txt
labs/lab-16/data/KDDTrain+.txt
tiny-ai-site.zip
venv/

# workflow scratch worktrees
.claude/worktrees/
